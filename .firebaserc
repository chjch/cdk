{
  "projects": {
    "default": "dcp-fiber"  // cloud run project name
  },
  "targets": {
      "dcp-fiber": {    // firebase project name
        "hosting": {
          "cdkdash": [ // cloud run service name
            "dcp-fiber" // firebase project name
          ]
        }
      }
    },
    "etags": {}
}